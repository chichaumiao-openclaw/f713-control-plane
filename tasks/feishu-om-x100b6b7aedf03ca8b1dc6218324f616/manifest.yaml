schema_version: 1
task_id: feishu-om-x100b6b7aedf03ca8b1dc6218324f616
title: "Feishu Hermes task: summarize current f713-control-plane status and report back here after the lates"
task_class: engineering_simple
project_ref: feishu
created_by: feishu
launch:
  mode: codex_exec
  working_dir: /hdd/chichau/current_projects
  entrypoint: ""
  prompt: "You are running on f713 as a Hermes task triggered from Feishu. Complete the user's request fully in the current repository context if possible. When done, write a concise final summary suitable for sending back to Feishu.\n\nUser request from Feishu:\nsummarize current f713-control-plane status and report back here after the latest restart\n"
  codex_profile: agnes
inputs: []
expected_outputs: []
completion_checks:
  - "worker exits 0"
watchdog:
  enabled: true
  interval_min: 5
  max_retries: 3
  stall_cutoff: 2
notify:
  feishu: true
human_gate:
  required: false
